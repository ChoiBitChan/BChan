package com.chan.home.study.word;

public class Study_Word_Moneyput {

	int count; // 멤버변수

	static int total; // 멤버변수

	public void sum(int money) { // 메소드 명 : sum, 매개변수 : int money

		count = count + money; // 멤버변수(count)에 매개변수(money)에 들어온 인자값을 입력

		total = total + money; // 멤버변수(count)에 매개변수(money)에 들어온 인자값을 입력

	}

}
